[project]
name = "llm-review-mcp"
version = "0.1.0"
description = "MCP server for llm-wiki cross-model Review LLM calls."
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27,<1.0",
]

[tool.uv]
package = false

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
