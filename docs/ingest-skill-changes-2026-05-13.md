# /ingest skill changes — 2026-05-13

Changelog for targeted modifications to `/ingest`, motivated by the cross-model wiki comparison documented in `wiki_comparison.md` (May 13, 22:41 reread of `YenochWiki/wiki/`).

## Motivation

The May 13 comparison concluded that `YenochWiki/wiki/` (the run generated by the current `/ingest`) is the structurally strongest of five candidate wikis on internal-link cleanliness, concept coverage, formula completeness, claim-evidence layering, and frontmatter regularity. Three gaps remained:

1. **Broken `[prepared markdown]` links.** `wiki/sources/papers/`, `sources/notes/`, and `sources/web/` were emptied (replaced with `.gitkeep`) at 22:45, four minutes after the 22:41 ingest finished. The concept pages had already written `[prepared markdown](../sources/papers/<source-slug>.md)` references to files that no longer exist. `/ingest` itself did not delete the sources, but it also did not verify the link targets at end-of-skill, so the dead links shipped silently.
2. **Generic `## My understanding` sections.** Concept pages read like neutral textbook stubs ("MCDHF is the workhorse method for precision relativistic atomic structure calculations…"). `wiki_back2` was the only candidate that explicitly tied concepts to the user's active research direction (ML CSF selection / GRASP2018) — e.g. flagging CSF-structure factors as natural ML descriptor features. The current skill rubric says `## My understanding` is mandatory but does not say what it must contain.
3. **`topics/` silently left empty.** `wiki/topics/`, `Summary/`, `experiments/`, `foundations/`, `ideas/`, and `outputs/` were all empty directories. `/ingest` already declines to create topic pages (correctly — that belongs to `/init` and `/edit`), but it also did not surface the zero-match outcome to the user, who had no signal that the SOTA-tracking layer was missing.

The first three changes below address these gaps without expanding `/ingest`'s remit (it does not start creating topic pages or auto-fixing source loss). A follow-up implementation pass also aligned `/ingest` with the repository's `uv run python -X utf8` and `config/paths.json` conventions, and hardened it against directory-prefixed wikilinks in Obsidian vaults that contain multiple candidate wiki directories.

## Change 1 — Self-check: prepared-source link targets must resolve

**Edits**:
- `i18n/zh/skills/ingest/SKILL.md` — Step 8 self-check: added item 8.
- `i18n/en/skills/ingest/SKILL.md` — added a self-check section (EN previously had none); the prepared-source check is item 8 in the final 10-item list shared with the ZH side.
- `i18n/zh/skills/ingest/references/content-quality-gate.md` — Post-ingest verification checklist: added item 8.

**New rule**: every `[prepared markdown](../sources/papers/<source-slug>.md)` link written by the current ingest must point at a file under `@configured-sources-papers/` that exists and has size > 0. If any target is missing or empty, surface the affected source slugs in the report and stop instead of shipping dead links. Concept pages without source backing must use the documented `prepared markdown: missing` fallback wording — never a live link to an empty file.

**What this does not do**: it does not investigate or roll back the upstream cause (`/reset --scope wiki`, `setup.sh` rerun, `tools/clean_wiki_repository.py`, manual `rm`, etc.). Diagnosing which tool emptied `wiki/sources/` is out of scope; the check exists so the next ingest fails fast instead of silently shipping broken links.

## Change 2 — Concept `## My understanding` must anchor to user research direction

**Edits**:
- `i18n/zh/skills/ingest/SKILL.md` — Step 4 prelude: new "Research-direction anchor" paragraph. Step 4 item 7: expanded `## My understanding` requirement.
- `i18n/en/skills/ingest/SKILL.md` — Step 4 prelude added; Step 4 item 7 covers the same requirement after the claim-floor item.
- `i18n/zh/skills/ingest/references/content-quality-gate.md` — Concept page floor: expanded `## My understanding` description.
- Step 8 self-check (both languages): added an item verifying the connection sentence is present, or has a one-line scoped reason, or notes the anchor file was absent.

**New rule**:
- Before drafting any concept page's `## My understanding`, the skill reads `@configured/Summary/research-direction.md` if it exists. The file is a user-maintained free-form Markdown page that lists the active research direction(s) — e.g. `ML CSF selection`, `GRASP2018 high-precision atomic structure`. No schema is imposed; the skill consumes it as anchoring context.
- `## My understanding` must contain **at least one concrete connection sentence** tying the concept to one of those directions — how the concept appears in that direction and what role it plays (descriptor feature, computational bottleneck, validation benchmark, baseline method, …).
- If the source paper genuinely cannot defend a connection, the skill writes a one-line scoped reason instead of forcing a generic tie-in. This is the explicit escape hatch — the rule is "anchor when defensible", not "anchor at any cost".
- If `@configured/Summary/research-direction.md` does not exist, the skill writes a generic maintainer-voice synthesis and appends `_no research-direction anchor file found_` on its own line so future agents see why the connection sentence is missing.

**Configuration surface**: `@configured/Summary/research-direction.md` is the canonical anchor. There is no flag to disable this behavior; absence of the file is itself the off switch. Users who want to tighten or broaden the connection requirement should edit `research-direction.md` content, not the skill.

**What this does not do**: it does not retro-edit existing concept pages — the rule applies to new and materially-edited concept pages written by future ingest runs. Existing pages with generic `## My understanding` sections will be flagged by `/check` on its next sweep, not by `/ingest`.

## Change 3 — Topic placement count must be surfaced in the report

**Edits**:
- `i18n/zh/skills/ingest/SKILL.md` — Step 6 item 1 (record matched topic count `N`), Step 6 item 2 (surface `N=0` case), Step 8 report content list.
- `i18n/en/skills/ingest/SKILL.md` — mirror edits in Step 6 and Step 8.
- `i18n/zh/skills/ingest/references/content-quality-gate.md` — Post-ingest verification checklist item 10.

**New rule**: Step 6 already matches the paper's domain/tags against `@configured/topics/*.md`. The skill now also records the matched count `N` and includes a `Topic placement: matched N topics` line in the Step 8 report. When `N=0`, the report appends a one-line suggestion to run `/edit` and create a topic page for the paper's domain. The skill still does not create topic pages itself.

## Change 4 — Runtime paths and Python invocation follow repository conventions

**Edits**:
- `i18n/zh/skills/ingest/SKILL.md`
- `i18n/en/skills/ingest/SKILL.md`

**New rule**:
- Python tools are invoked with `uv run python -X utf8`, matching `README.md`.
- `/ingest` runs from the repository root and lets each tool resolve runtime paths through `tools/_paths.py`.
- `config/paths.json`, `LLM_WIKI_WIKI_ROOT`, `LLM_WIKI_RAW_ROOT`, and `LLM_WIKI_PATH_PROFILE` are the default source of truth for runtime paths.
- Commands and prose use `@configured`, `@raw-root`, and related aliases rather than hard-coded `wiki/` and `raw/`.

This prevents future ingest runs from silently writing into the repository-local `wiki/` when the user configured an external Obsidian vault.

## Change 5 — Directory-prefixed wikilinks are forbidden

**Edits**:
- `i18n/zh/skills/ingest/SKILL.md`
- `i18n/en/skills/ingest/SKILL.md`
- `i18n/zh/skills/ingest/references/content-quality-gate.md`

**New rule**: `/ingest` must emit slug-only wikilinks such as `[[dirac-hartree-fock]]`. It must not emit directory-prefixed wikilinks such as `[[wiki/...]]`, `[[wiki_glm/...]]`, `[[wiki_back.../...]]`, or `[[topics/slug]]`. Prepared source provenance remains an ordinary relative markdown link, not a wikilink.

This does not claim Obsidian cannot later rewrite links when multiple same-named candidate wiki directories live in one vault. It only guarantees `/ingest` itself does not introduce that pollution.

## Change 6 — Summary placement is reported alongside topic placement

**Edits**:
- `i18n/zh/skills/ingest/SKILL.md`
- `i18n/en/skills/ingest/SKILL.md`
- `i18n/zh/skills/ingest/references/content-quality-gate.md`

**New rule**: `/ingest` may update an existing `@configured/Summary/*.md` page when its scope clearly covers the paper, but it still does not create Summary pages. The final report includes `Summary placement: matched S summaries`; if `S=0`, it suggests adding or updating a Summary page.

## Files touched

```
i18n/zh/skills/ingest/SKILL.md
i18n/en/skills/ingest/SKILL.md
i18n/zh/skills/ingest/references/content-quality-gate.md
docs/ingest-skill-changes-2026-05-13.md  (this file)
```

The `i18n/en/skills/ingest/references/` directory does not contain `content-quality-gate.md` and was not extended in this change — the EN side keeps its requirements inline in `SKILL.md`.

## Verification on existing wiki

These changes do not retroactively fix the May 13 ingest. To bring `YenochWiki/wiki/` up to the new floor:

1. Restore the 3050-line MinerU-converted chapter Markdown at `YenochWiki/wiki/sources/papers/relativistic-quantum-theory-atoms-molecules-chapter.md` — either re-run `/ingest-local-pdf` or copy from one of the other wiki candidates that still has it.
2. Create `YenochWiki/wiki/Summary/research-direction.md` describing the active research direction (ML CSF selection on GRASP2018).
3. Run `/reingest relativistic-quantum-theory-atoms-molecules-chapter` so the concept pages pick up the new `## My understanding` rule and the topic-placement report line.

The order matters: step 1 must complete before step 3, otherwise step 3 will trigger the prepared-source and research-direction self-checks.

## Out of scope

The comparison surfaces two further gaps that this change deliberately does not address:

- **Cross-direction horizontal links** (atomic physics ↔ exoplanet metallicity, as seen in `wiki_back`). Single-paper `/ingest` does not have the cross-domain context to add these; they belong to `/edit`, `/survey`, or a periodic `/check` sweep.
- **Filling `topics/`, `Summary/`, `experiments/`, `foundations/`, `ideas/`, `outputs/`**. These are `/init` and `/edit` responsibilities. `/ingest` now surfaces the `topics/` gap in the report; the rest stays the user's call.
